definitions:
  notFound:
    type: "object"
    properties:
      statusCode:
        type: "integer"
        example: "404"
      message:
        type: "string"
        example: "the requested user is not present"
